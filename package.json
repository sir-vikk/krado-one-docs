{
  "name": "krado-one-docs",
  "private": true,
  "scripts": {
    "dev:website": "npx serve . -p 3000 -n",
    "dev:docs": "cd docs && npx vitepress dev --port 5173",
    "build": "cd docs && npm run build && node ../scripts/assemble.mjs",
    "preview": "npm run build && npx serve _site -p 4000"
  }
}
